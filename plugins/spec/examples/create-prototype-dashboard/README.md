# Prototype Dashboard

This is the full reference example for a spec create plugin. It includes a portable `SKILL.md`, OD sidecar, preview, seed asset, and eval file.

```bash
od-cli plugin validate .
od-cli plugin install .
od-cli plugin apply create-prototype-dashboard --input audience="support leads" --input topic="enterprise onboarding risk"
```

